Statistical methods 1 README
You may use Rstudio or Posit Cloud
Open statsfilter.R and run the code (make sure the bodyfat dataset is in the same directory"
REQUIRED PACKAGES:
"dplyr"
"forcats"
"readxl"
"grDevices"
"ggplot2"
use install.packages"(package)" to install and
library(package) to load specified packages
Install as needed as well as any dependencies
Graphs and analyses should show up when code is ran assuming the dataset is loaded correctly. 
 